拟合原函数时，发现10到30个样本已经能基本还原

优化超参数的搜索空间很重要，太大，会导致失败

高斯过程均值函数中的超参数要显示表达出来，不然会优化不了

噪声方差太大，会影响源函数的收敛

九十度策略中，不是单个步长距离越大越好，实验发现单个步长为0.5时，效果不错

simpleRule选取候选集合，使用每个样本都<=9*delta**2成立，候选集合缩小显著，而之前使用的策略并没有显著减小候选集合,3delta准则对算法准确度有很大的影响。

做速度估计时，deltaT是不能太小的，同时样本数量不能过于少，这里是0.1对应k取60