拟合原函数时，发现10到30个样本已经能基本还原

优化超参数的搜索空间很重要，太大，会导致失败

高斯过程均值函数中的超参数要显示表达出来，不然会优化不了

噪声方差太大，会影响源函数的收敛

因为分布函数的形式我们其实是知道的，mean function 直接定义成相应的形式，效果会很好，即使在噪声比较大的情况下,三四步采样数据后，就能得到源的位置。

采集到的样本如果过于集中，就可能会陷入局部最优解，应该使得采集过程中，样本不限定在一个小的区域，而样本之间的最小差异为多少时能保证收敛则显得尤为重要。